if(VIVID_BINDING_FREERTOS)
    add_subdirectory(freertos)
endif()
if(VIVID_BINDING_LIBEV)
    add_subdirectory(libev)
endif()
if(VIVID_BINDING_WIN32)
    add_subdirectory(win32)
endif()
if(VIVID_BINDING_LINUX)
    add_subdirectory(linux)
endif()
